# Closeout pass — five branches to one

**Date:** 2026-09-03
**Result branch:** `feature/qa-500-tests` — **261 tests, all green. `makemigrations --check`: no changes detected.**
**Not pushed. Not merged to `main`.**

---

## Task 1 — Finding J: detected missing, fixed

Grepped `apps/home/forms.py` first, as instructed, rather than assuming. **No size cap existed** on `AlumniDigitalIDApplicationForm.digital_id_photo` — confirmed by absence of any `clean_digital_id_photo`, `.size` check, or byte-count constant.

Added:

```python
def clean_digital_id_photo(self):
    photo = self.cleaned_data.get("digital_id_photo")
    if photo and photo.size > 2 * 1024 * 1024:
        raise forms.ValidationError("Photo must be 2 MB or smaller.")
    return photo
```

Required-ness untouched — still enforced at the field. Test: a genuine PNG (PIL-verified) padded past 2 MB with trailing zero bytes after `IEND`, which Pillow still reads as a valid image, so the rejection is exercised on a real oversized image rather than a malformed one.

**Committed on `fix/forms-fk`** — `7ddc068`. 224/224 green on that branch's own scope (pre-dates the QR-gen and finding-L work, which reconciled at the merge).

---

## Task 2 — E and H recorded as settled decisions

No `docs/decisions/` location existed; followed the repo's actual convention — flat, dated files in `docs/`, matching `ledger-decisions-2026-09-02.md`.

- [`docs/finding-E-refund-policy.md`](finding-E-refund-policy.md) — refunds do not reverse activation; membership ends only through deliberate Secretariat cancellation. Confirmed against the code: `mark_as_refunded`/`mark_as_failed` touch only `payment_status` and `notes`, never `Membership`.
- [`docs/finding-H-installment-activation.md`](finding-H-installment-activation.md) — first-instalment activation is amount-agnostic by design; the safeguard is Secretariat confirmation, not a floor.

**Committed on `coverage/phase-1`** — `29f0e12`.

---

## Task 3 — media/.gitignore: **STOPPED, not actioned**

`git ls-files media/` returned **114 already-tracked files**:

| Folder | Count |
|---|---:|
| `qr_codes/` | 83 |
| `banner/` | 19 |
| `program_areas/` | 6 |
| `employee_photos/` | 5 |
| `pdf/` | 1 |

Per the stop condition, I did **not** touch `.gitignore` and did **not** mass-remove anything. `git status` shows only two *untracked* media folders from before this pass (`media/banner/alumni_qr_watermark/2026/08/21/`, `media/scholarship_applications/`) — those aren't the issue; the 114 tracked files are.

**This needs a decision from you before it can close:**
- `git rm --cached` (114 files) removes them from tracking going forward but **leaves the working files in place** — the safe option, and the natural pairing with the finding-L migration, which moves these same files to Cloudinary anyway.
- Doing nothing is also fine short-term; it just means `git status` stays noisy and the repo keeps carrying binary history for files that are about to move off the VPS regardless.

I'd lean toward `git rm --cached` once the finding-L runbook has actually run and the Cloudinary copies are verified — untracking before the migration risks losing the only record of what was there if something goes wrong mid-run.

---

## Task 4 — orphan snippets: re-verified, deleted

Re-ran the reference sweep (`{% include %}`, `{% extends %}`, `render()`, `template_name=`) across every `.html` and `.py` file. One hit surfaced for `front_page_card` — inside a `{% comment %}` block in `templates/home/alumni_home.html:64`, describing a *design pattern* ("front_page_card.html's own pattern — image on top...") rather than an actual reference. Not a real usage.

All six confirmed still unreferenced:
`article.html`, `chapter_card.html`, `ec_profile_card.html`, `front_page_card.html`, `in_memoriam_card.html`, `secretariat_card.html`.

**Deleted directly (`rm`), not `git rm`** — they were untracked (`??` in `git status`), so there was nothing in git history to remove and no commit was possible or needed. `git status` confirms they're simply gone now, with no trace in the diff.

---

## Task 5 — the five-branch stack, merged to one

All three `--no-ff` merges into `coverage/phase-1`, then that into `feature/qa-500-tests`. Suite run after every step; no step was papered over.

| Merge | Commit | Conflict? | Suite after |
|---|---|---|---|
| `fix/finding-d-payment-activation` → `coverage/phase-1` | `283bd23` | Yes — see below | 250 green |
| `fix/forms-fk` → `coverage/phase-1` | `8feb49f` | Yes — see below | 253 green |
| `fix/finding-l-storage` → `coverage/phase-1` | `13af260` | Yes — see below | 261 green |
| `coverage/phase-1` → `feature/qa-500-tests` | `4980132` | **No** — clean `ort` auto-merge | 261 green |

### The conflict pattern, all three times

Every conflict was in `apps/home/tests.py`, and every one had the **same shape**: two branches had each independently appended a new test block right after the same anchor point in a shared ancestor commit (the tail of the forms-coverage block). Git can't interleave two additive appends at the same location, so it flags the whole span as conflicting even though nothing actually contradicts.

None were resolved by picking a side. Each was resolved by **reading both blocks in full, confirming no test method, helper, or class name collided, and reconstructing the correct concatenation** — with one nesting subtlety on the second merge: finding J's new test method had to stay *inside* `AlumniDigitalIDApplicationFormTests` (it's a method, not a top-level append), so it couldn't simply be dropped at the textual conflict boundary — it had to be threaded back into the class before the next top-level block began. Getting that wrong would have been a silent `IndentationError`, not a subtle bug, so it would have surfaced immediately — but it was checked explicitly rather than left to chance.

`apps/home/models.py` (the `expires_on` caveat comment) and `apps/home/admin.py` auto-merged with no markers at all.

Verified after each merge, not assumed:
- No conflict markers left anywhere in the tree (`grep` swept the whole repo, not just the touched files).
- No duplicate class definitions (`_StubChangeForm`, `PaymentActivationOnCompletionTests`, `AlumniRegisterViewFormKwargsTests`, `MigrateMediaCommandTests` — each appears exactly once).
- Full suite green, not just the touched test classes.

---

## Final state

```
feature/qa-500-tests   261 tests, all green
                        makemigrations --check: no changes detected
                        not pushed, not merged to main
```

Branches unchanged otherwise — nothing deleted. `fix/finding-d-payment-activation`, `fix/forms-fk`, and `fix/finding-l-storage` are now fully contained in `feature/qa-500-tests` (`merge-base --is-ancestor` confirms all three), so they're safe to delete once you're satisfied, but I left that decision to you rather than assuming it.

---

## Ledger — final position

| # | Finding | State |
|---|---|---|
| A, C, G | — | Retracted |
| B, D, F, K | — | Fixed |
| **J** | **No size limit on Digital ID photo** | **✅ Fixed this pass (`7ddc068`)** |
| **E** | **Refund does not reverse activation** | **✅ Settled as intended (this pass)** |
| **H** | **1-cent instalment activates any tier** | **✅ Settled as intended (this pass)** |
| I, M, N, O–R | — | Documented |
| **L** | **`STORAGES` unset** | **Code fixed; host runbook still pending — production-only** |

**Every ledger item is now either fixed, retracted, documented, or explicitly settled.** Nothing is left in a genuinely open, undecided state.

---

## What's still outside this pass

1. **The 114 tracked media files** — needs your call (Task 3, above).
2. **The finding-L runbook** — production-host steps, unchanged, still pending: [`finding-L-runbook-2026-09-03.md`](finding-L-runbook-2026-09-03.md).
3. **Deleting the three now-merged branches** — safe once you're ready; left undone by default.
4. **Coverage priorities 7 (`home`/`staff` views POST handling), 9 (`tasks.py`), 10 (`import_legacy_memberships`)** — not started, per this pass's explicit scope.
